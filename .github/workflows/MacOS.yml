name: MacOs (app and dmg)

on:
  workflow_dispatch:
  workflow_call:

env:
  PROJECT: Pymca_release_test
  PROJECT_LOWER: pymca_release_test

jobs:
# Fat binaries
  macos_arm_pyinstaller_python312:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.12"

      - name: Install Rosetta
        run: |
          sudo softwareupdate --install-rosetta --agree-to-license

      - name: Set environment for 10.13 compatibility #10.13 is a minimum - setting 10.9 do not actually work
        run: |
          echo "MACOSX_DEPLOYMENT_TARGET=10.13" >> $GITHUB_ENV
          echo "CFLAGS=-mmacosx-version-min=10.13" >> $GITHUB_ENV
          echo "LDFLAGS=-mmacosx-version-min=10.13" >> $GITHUB_ENV

      - name: Install dependencies
        run: |
          pip install "setuptools<81"
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/numpy-1.26.2-cp312-cp312-macosx_10_9_universal2.whl
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/h5py-3.10.0-cp312-cp312-macosx_11_0_universal2.whl
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/psutil-5.9.6-cp312-abi3-macosx_10_9_universal2.whl
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/Pillow-10.1.0-cp312-cp312-macosx_11_0_universal2.whl
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/tornado-6.3.3-cp38-abi3-macosx_10_9_universal2.whl
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/contourpy-1.2.0-cp312-cp312-macosx_11_0_universal2.whl
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/kiwisolver-1.4.5-cp312-cp312-macosx_11_0_universal2.whl
          pip install cycler==0.12.1
          pip install fonttools==4.45.1
          pip install packaging==23.2
          pip install pyparsing==3.1.1
          pip install python_dateutil==2.8.2
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/matplotlib-3.8.2-cp312-cp312-macosx_11_0_universal2.whl
          pip install h5py
          pip install hdf5plugin
          pip install PySide6==6.6.0
          pip install lxml==4.9.3
          pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/fabio-2023.10.0-cp312-cp312-macosx_10_9_universal2.whl
          pip install fisx
          pip install git+https://github.com/silx-kit/silx.git
          pip install PyOpenGL
          pip install qtconsole==5.5.1
          pip install PyInstaller==6.2.0
          pip install Cython
          pip install wheel
          pip install --pre --index-url https://test.pypi.org/simple/ --trusted-host test.pypi.org --only-binary Pymca_release_test Pymca_release_test
          pip install git+https://github.com/vasole/bcflight.git


      - name: Build PyInstaller app for arm64
        run: |
          arch -arm64 python build-pyinstaller.py
          ls dist
          find dist/ -type f -perm +111 | while read -r file; do
            version=$(otool -l "$file" 2>/dev/null | awk '/LC_VERSION_MIN_MACOSX/ {show=1} show && /version / {print $2; show=0}' | head -n 1)
            if [ -n "$version" ]; then
              echo "$version  <-  $file"
            fi
          done | sort

      - name: Set app name variable
        run: |
          APP_NAME=$(find dist -maxdepth 1 -name 'PyMca*.app' | head -n 1 | xargs -n 1 basename)
          echo "APP_NAME=$APP_NAME" >> $GITHUB_ENV

      ### correct and test app file

      - name: Move .app to /Applications
        run: |
          sudo mv ./dist/${{ env.APP_NAME }} /Applications/
          ls -l /Applications/${{ env.APP_NAME }}/Contents/MacOS

      - name: Make main binary executable
        run: chmod +x /Applications/${{ env.APP_NAME }}/Contents/MacOS/PyMcaMain

      - name: Rename __dot__dylibs folders to .dylibs
        run: |
          find /Applications/${{ env.APP_NAME }} -depth -type d -name '__dot__dylibs' | while IFS= read -r dir; do
            parent_dir=$(dirname "$dir")
            target="$parent_dir/.dylibs"

            if [ -e "$target" ]; then
              echo "⚠️ Skipping: $target already exists."
            else
              mv "$dir" "$target"
              if [ -d "$target" ]; then
                if [ "$(ls -A "$target")" ]; then
                  echo "✅ Renamed and verified: $dir → $target (not empty)"
                else
                  echo "⚠️ Renamed: $dir → $target but it's empty"
                fi
              else
                echo "❌ Rename failed: $dir → $target does not exist"
              fi
            fi
          done

      - name: Run the app to check it launches
        run: |
          set -e
          
          echo "Launching PyMcaMain..."
          LOGFILE=$(mktemp)
          
          # Start the app in the background
          arch -arm64 /Applications/${{ env.APP_NAME }}/Contents/MacOS/PyMcaMain > "$LOGFILE" 2>&1 &
          APP_PID=$!
          
          echo "App started with PID $APP_PID"
          
          # Wait up to 120 seconds for it to crash or hang
          for i in {1..120}; do
            if ! ps -p $APP_PID > /dev/null; then
              echo "App exited early. Checking exit code..."
              wait $APP_PID || true
              EXIT_CODE=$?
              echo "Exit code: $EXIT_CODE"
              echo "App output log:"
              cat "$LOGFILE"
              if [ $EXIT_CODE -ne 0 ]; then
                echo "App exited with error code $EXIT_CODE"
                exit $EXIT_CODE
              else
                echo "App exited cleanly before 2 minutes."
                exit 0
              fi
            fi
          
            if grep -qE 'ImportError|Traceback|ERROR:' "$LOGFILE"; then
              echo "Detected error in application output:"
              cat "$LOGFILE"
              echo "Failing due to runtime error."
              kill $APP_PID || true
              wait $APP_PID || true
              exit 1
            fi
          
            sleep 1
          done
          
          echo "App ran for 2 minutes without crashing. Killing it now..."
          kill $APP_PID || true
          wait $APP_PID || true
          echo "App test complete."

      - name: Copy .app out /Applications
        run: |
          sudo mkdir -p myappfolder
          sudo cp -R "/Applications/${{ env.APP_NAME }}" myappfolder/

      - name: Cleanup
        run: |
          sudo rm -rf /Applications/${{ env.APP_NAME }}

      ### dmg test

      - name: Mount the DMG
        run: |
          DMG_NAME="${APP_NAME/.app/.dmg}"
          hdiutil attach "dist/$DMG_NAME" -mountpoint /Volumes/PyMcaApp
        env:
          APP_NAME: ${{ env.APP_NAME }}

      - name: Copy .app to /Applications
        run: |
          sudo cp -R "/Volumes/PyMcaApp/${{ env.APP_NAME }}" /Applications/

      - name: Make main binary executable
        run: chmod +x /Applications/${{ env.APP_NAME }}/Contents/MacOS/PyMcaMain

      - name: Run the app to check it launches
        run: |
          set -e
          
          echo "Launching PyMcaMain..."
          LOGFILE=$(mktemp)
          
          # Start the app in the background
          arch -arm64 /Applications/${{ env.APP_NAME }}/Contents/MacOS/PyMcaMain > "$LOGFILE" 2>&1 &
          APP_PID=$!
          
          echo "App started with PID $APP_PID"
          
          # Wait up to 120 seconds for it to crash or hang
          for i in {1..120}; do
            if ! ps -p $APP_PID > /dev/null; then
              echo "App exited early. Checking exit code..."
              wait $APP_PID || true
              EXIT_CODE=$?
              echo "Exit code: $EXIT_CODE"
              echo "App output log:"
              cat "$LOGFILE"
              if [ $EXIT_CODE -ne 0 ]; then
                echo "App exited with error code $EXIT_CODE"
                exit $EXIT_CODE
              else
                echo "App exited cleanly before 2 minutes."
                exit 0
              fi
            fi
          
            if grep -qE 'ImportError|Traceback|ERROR:' "$LOGFILE"; then
              echo "Detected error in application output:"
              cat "$LOGFILE"
              echo "Failing due to runtime error."
              kill $APP_PID || true
              wait $APP_PID || true
              exit 1
            fi
          
            sleep 1
          done
          
          echo "App ran for 2 minutes without crashing. Killing it now..."
          kill $APP_PID || true
          wait $APP_PID || true
          echo "App test complete."

      - name: Cleanup
        run: |
          sudo rm -rf /Applications/${{ env.APP_NAME }}

      # upload .dmg
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm-pyinstaller-artifacts-dmg
          path: dist/*.dmg
          retention-days: 1

      # upload .app as well
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm-pyinstaller-artifacts-dashdotfix
          path: myappfolder/*
          include-hidden-files: true
          retention-days: 1

  macos_x86_pyinstaller_python312:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.12"

      - name: Install Rosetta
        run: |
          sudo softwareupdate --install-rosetta --agree-to-license

      - name: Set environment for 10.13 compatibility #10.13 is a minimum - setting 10.9 do not actually work
        run: |
          echo "MACOSX_DEPLOYMENT_TARGET=10.13" >> $GITHUB_ENV
          echo "CFLAGS=-mmacosx-version-min=10.13" >> $GITHUB_ENV
          echo "LDFLAGS=-mmacosx-version-min=10.13" >> $GITHUB_ENV

      # "castrate" interpreter to have only x86
      - name: Setup MacOS x86_64 Python
        run: |
          arch -x86_64 python -m venv venv-x86
          lipo venv-x86/bin/python -remove arm64 -output venv-x86/bin/python_x86
          sudo mv venv-x86/bin/python_x86 venv-x86/bin/python
          chmod +x venv-x86/bin/python
          file venv-x86/bin/python
          source venv-x86/bin/activate

      - name: Install dependencies
        run: |
          source venv-x86/bin/activate
          arch -x86_64 pip install "setuptools<81"
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/numpy-1.26.2-cp312-cp312-macosx_10_9_universal2.whl
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/h5py-3.10.0-cp312-cp312-macosx_11_0_universal2.whl
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/psutil-5.9.6-cp312-abi3-macosx_10_9_universal2.whl
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/Pillow-10.1.0-cp312-cp312-macosx_11_0_universal2.whl
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/tornado-6.3.3-cp38-abi3-macosx_10_9_universal2.whl
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/contourpy-1.2.0-cp312-cp312-macosx_11_0_universal2.whl
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/kiwisolver-1.4.5-cp312-cp312-macosx_11_0_universal2.whl
          arch -x86_64 pip install cycler==0.12.1
          arch -x86_64 pip install fonttools==4.45.1
          arch -x86_64 pip install packaging==23.2
          arch -x86_64 pip install pyparsing==3.1.1
          arch -x86_64 pip install python_dateutil==2.8.2
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/matplotlib-3.8.2-cp312-cp312-macosx_11_0_universal2.whl
          arch -x86_64 pip install h5py
          arch -x86_64 pip install hdf5plugin
          arch -x86_64 pip install PySide6==6.6.0
          arch -x86_64 pip install lxml==4.9.3
          arch -x86_64 pip install --no-cache --trusted-host www.silx.org http://www.silx.org/pub/wheelhouse/universal2/fabio-2023.10.0-cp312-cp312-macosx_10_9_universal2.whl
          arch -x86_64 pip install fisx
          arch -x86_64 pip install git+https://github.com/silx-kit/silx.git
          arch -x86_64 pip install PyOpenGL
          arch -x86_64 pip install qtconsole==5.5.1
          arch -x86_64 pip install PyInstaller==6.2.0
          arch -x86_64 pip install Cython
          arch -x86_64 pip install wheel
          arch -x86_64 pip install --pre --index-url https://test.pypi.org/simple/ --trusted-host test.pypi.org --only-binary Pymca_release_test Pymca_release_test
          arch -x86_64 pip install git+https://github.com/vasole/bcflight.git

      - name: Build PyInstaller app for x86_64
        run: |
          source venv-x86/bin/activate
          arch -x86_64 python build-pyinstaller.py
          ls dist
          find dist/ -type f -perm +111 | while read -r file; do
            version=$(otool -l "$file" 2>/dev/null | awk '/LC_VERSION_MIN_MACOSX/ {show=1} show && /version / {print $2; show=0}' | head -n 1)
            if [ -n "$version" ]; then
              echo "$version  <-  $file"
            fi
          done | sort

      - name: Set app name variable
        run: |
          APP_NAME=$(find dist -maxdepth 1 -name 'PyMca*.app' | head -n 1 | xargs -n 1 basename)
          echo "APP_NAME=$APP_NAME" >> $GITHUB_ENV

      ### dmg test

      - name: Mount the DMG
        run: |
          DMG_NAME="${APP_NAME/.app/.dmg}"
          hdiutil attach "dist/$DMG_NAME" -mountpoint /Volumes/PyMcaApp
        env:
          APP_NAME: ${{ env.APP_NAME }}

      - name: Copy .app to /Applications
        run: |
          sudo cp -R "/Volumes/PyMcaApp/${{ env.APP_NAME }}" /Applications/

      - name: Make main binary executable
        run: chmod +x /Applications/${{ env.APP_NAME }}/Contents/MacOS/PyMcaMain

      - name: Run the app to check it launches
        run: |
          set -e
          
          echo "Launching PyMcaMain..."
          LOGFILE=$(mktemp)
          
          # Start the app in the background
          arch -x86_64 /Applications/${{ env.APP_NAME }}/Contents/MacOS/PyMcaMain > "$LOGFILE" 2>&1 &
          APP_PID=$!
          
          echo "App started with PID $APP_PID"
          
          # Wait up to 120 seconds for it to crash or hang
          for i in {1..120}; do
            if ! ps -p $APP_PID > /dev/null; then
              echo "App exited early. Checking exit code..."
              wait $APP_PID || true
              EXIT_CODE=$?
              echo "Exit code: $EXIT_CODE"
              echo "App output log:"
              cat "$LOGFILE"
              if [ $EXIT_CODE -ne 0 ]; then
                echo "App exited with error code $EXIT_CODE"
                exit $EXIT_CODE
              else
                echo "App exited cleanly before 2 minutes."
                exit 0
              fi
            fi
          
            if grep -qE 'ImportError|Traceback|ERROR:' "$LOGFILE"; then
              echo "Detected error in application output:"
              cat "$LOGFILE"
              echo "Failing due to runtime error."
              kill $APP_PID || true
              wait $APP_PID || true
              exit 1
            fi
          
            sleep 1
          done
          
          echo "App ran for 2 minutes without crashing. Killing it now..."
          kill $APP_PID || true
          wait $APP_PID || true
          echo "App test complete."

      - name: Cleanup
        run: |
          sudo rm -rf /Applications/${{ env.APP_NAME }}

      # upload .dmg
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-x86(intel)-pyinstaller-artifacts-dmg
          path: dist/*.dmg
          retention-days: 1


### forbidden by Marius
#  macos_x86_arm64_in_one_python312:
#    runs-on: macos-latest
#    needs:
#      - macos_arm_pyinstaller_python312
#      - macos_x86_pyinstaller_python312
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v2
#
#      - name: Setup Python
#        uses: actions/setup-python@v2
#        with:
#          python-version: "3.12"
#
#      - name: Download Mac arm64 artifact
#        uses: actions/download-artifact@v4
#        with:
#          name: macos-arm-pyinstaller-artifacts-dmg
#          path: dist
#
#      - name: rename dmg
#        run: sudo mv dist/PyMca5*.dmg dist/PyMca_arm64.dmg
#
#      - name: Download Mac x86 artifact
#        uses: actions/download-artifact@v4
#        with:
#          name: macos-x86(intel)-pyinstaller-artifacts-dmg
#          path: dist
#
#      - name: rename dmg
#        run: sudo mv dist/PyMca5*.dmg dist/PyMca_x86.dmg
#
#      - name: Create working directory
#        run: |
#          mkdir -p PyMcaInstaller
#          mkdir PyMca
#
#      - name: Mount arm64 dmg
#        run: |
#          hdiutil attach dist/PyMca_arm64.dmg -mountpoint /Volumes/PyMcaARM
#          find /Volumes/PyMcaARM -mindepth 1 -maxdepth 1 -type d
#          app_path=$(find /Volumes/PyMcaARM -name 'PyMca*.app' -type d | head -n 1)
#          rm -rf PyMcaInstaller/PyMca-arm64.app
#          ditto "$app_path" "PyMcaInstaller/PyMca-arm64.app"
#          hdiutil detach /Volumes/PyMcaARM
#
#      - name: Mount x86 dmg
#        run: |
#          hdiutil attach dist/PyMca_x86.dmg -mountpoint /Volumes/PyMcaX86
#          find /Volumes/PyMcaX86 -mindepth 1 -maxdepth 1 -type d
#          app_path=$(find /Volumes/PyMcaX86 -name 'PyMca*.app' -type d | head -n 1)
#          rm -rf PyMcaInstaller/PyMca-x86_64.app
#          ditto "$app_path" "PyMcaInstaller/PyMca-x86_64.app"
#          hdiutil detach /Volumes/PyMcaX86
#
#      - name: List PyMcaInstaller contents
#        run: ls -l PyMcaInstaller
#
#      - name: Create install.command
#        run: |
#          cat << 'EOF' > PyMcaInstaller/install.command
#          #!/bin/bash
#
#          ARCH=$(uname -m)
#          echo "Detected architecture: $ARCH"
#
#          if [ "$ARCH" = "arm64" ]; then
#              APP_NAME="PyMca-arm64.app"
#          elif [ "$ARCH" = "x86_64" ]; then
#              APP_NAME="PyMca-x86_64.app"
#          else
#              echo "Unsupported architecture: $ARCH"
#          #    osascript -e 'display dialog "Unsupported architecture detected." buttons {"OK"}'
#              exit 1
#          fi
#
#          SRC="$(cd "$(dirname "$0")"; pwd)/$APP_NAME"
#          DST="/Applications/PyMca.app"
#
#          echo "Installing $APP_NAME to $DST"
#
#          # Ask user for permission via AppleScript dialog
#          osascript <<EOA
#          tell application "System Events"
#              activate
#              display dialog "Install PyMca to /Applications?" buttons {"Cancel", "Install"} default button "Install"
#          end tell
#          EOA
#
#          # Perform install
#          if cp -R "$SRC" "$DST"; then
#              echo "Installed successfully."
#          #    osascript -e 'display notification "PyMca installed to /Applications" with title "Installation Complete"'
#              open "$DST"
#          else
#              echo "Installation failed."
#          #    osascript -e 'display dialog "Failed to install PyMca. Check permissions." buttons {"OK"}'
#              exit 1
#          fi
#          EOF
#
#      - name: Make install.command executable
#        run: chmod +x PyMcaInstaller/install.command
#
#      - name: Create final DMG
#        run: |
#          hdiutil create -volname "PyMcaInstaller" \
#                        -srcfolder "PyMcaInstaller" \
#                        -ov -format UDZO "PyMca-Universal.dmg"
#
#      - name: Upload final universal DMG
#        uses: actions/upload-artifact@v4
#        with:
#          name: PyMca-Universal
#          path: PyMca-Universal.dmg
#
#      - name: Mount final DMG
#        run: |
#          hdiutil attach PyMca-Universal.dmg -mountpoint /Volumes/PyMcaInstaller
#
#      - name: Test ARM64 install script
#        run: |
#          echo "Testing install on arm64..."
#          arch -arm64 bash -c 'cd "/Volumes/PyMcaInstaller" && ./install.command'
#        continue-on-error: true  # Optional: Avoid failing the job if test fails
#
#      - name: Run the app to check it launches
#        run: |
#          set -e
#
#          echo "Launching PyMcaMain..."
#          LOGFILE=$(mktemp)
#
#          # Start the app in the background
#          arch -arm64 /Applications/${{ env.APP_NAME }}/Contents/MacOS/PyMcaMain > "$LOGFILE" 2>&1 &
#          APP_PID=$!
#
#          echo "App started with PID $APP_PID"
#
#          # Wait up to 120 seconds for it to crash or hang
#          for i in {1..120}; do
#            if ! ps -p $APP_PID > /dev/null; then
#              echo "App exited early. Checking exit code..."
#              wait $APP_PID || true
#              EXIT_CODE=$?
#              echo "Exit code: $EXIT_CODE"
#              echo "App output log:"
#              cat "$LOGFILE"
#              if [ $EXIT_CODE -ne 0 ]; then
#                echo "App exited with error code $EXIT_CODE"
#                exit $EXIT_CODE
#              else
#                echo "App exited cleanly before 2 minutes."
#                exit 0
#              fi
#            fi
#
#            if grep -qE 'ImportError|Traceback|ERROR:' "$LOGFILE"; then
#              echo "Detected error in application output:"
#              cat "$LOGFILE"
#              echo "Failing due to runtime error."
#              kill $APP_PID || true
#              wait $APP_PID || true
#              exit 1
#            fi
#
#            sleep 1
#          done
#
#          echo "App ran for 2 minutes without crashing. Killing it now..."
#          kill $APP_PID || true
#          wait $APP_PID || true
#          echo "App test complete."
#
#      - name: Intermediate step between arm64 and x86_64
#        run: |
#          echo "Running cleanup or prep step between ARM64 and x86_64 tests"
#          # For example, remove the app if it was installed
#          sudo rm -rf /Applications/PyMca.app || true
#
#      - name: Test x86_64 install script
#        run: |
#          echo "Testing install on x86_64..."
#          arch -x86_64 bash -c 'cd "/Volumes/PyMcaInstaller" && ./install.command'
#        continue-on-error: true
#
#      - name: Run the app to check it launches
#        run: |
#          set -e
#
#          echo "Launching PyMcaMain..."
#          LOGFILE=$(mktemp)
#
#          # Start the app in the background
#          arch -x86_64 /Applications/${{ env.APP_NAME }}/Contents/MacOS/PyMcaMain > "$LOGFILE" 2>&1 &
#          APP_PID=$!
#
#          echo "App started with PID $APP_PID"
#
#          # Wait up to 120 seconds for it to crash or hang
#          for i in {1..120}; do
#            if ! ps -p $APP_PID > /dev/null; then
#              echo "App exited early. Checking exit code..."
#              wait $APP_PID || true
#              EXIT_CODE=$?
#              echo "Exit code: $EXIT_CODE"
#              echo "App output log:"
#              cat "$LOGFILE"
#              if [ $EXIT_CODE -ne 0 ]; then
#                echo "App exited with error code $EXIT_CODE"
#                exit $EXIT_CODE
#              else
#                echo "App exited cleanly before 2 minutes."
#                exit 0
#              fi
#            fi
#
#            if grep -qE 'ImportError|Traceback|ERROR:' "$LOGFILE"; then
#              echo "Detected error in application output:"
#              cat "$LOGFILE"
#              echo "Failing due to runtime error."
#              kill $APP_PID || true
#              wait $APP_PID || true
#              exit 1
#            fi
#
#            sleep 1
#          done
#
#          echo "App ran for 2 minutes without crashing. Killing it now..."
#          kill $APP_PID || true
#          wait $APP_PID || true
#          echo "App test complete."
#
#      - name: Unmount final DMG
#        run: |
#          hdiutil detach /Volumes/PyMcaInstaller
          
